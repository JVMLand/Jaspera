import test from 'node:test';import assert from 'node:assert/strict';import {build} from 'esbuild';
const bundle=await build({entryPoints:['src/project-paths.ts'],bundle:true,write:false,platform:'node',format:'esm'});const {planPathChange}=await import('data:text/javascript;base64,'+Buffer.from(bundle.outputFiles[0].text).toString('base64'));
test('folder changes preserve relative paths',()=>assert.deepEqual([...planPathChange(['src/a/Main.jal','src/a/sub/Helper.jal','src/b/Other.jal'],'src/a','lib/a',true)],[['src/a/Main.jal','lib/a/Main.jal'],['src/a/sub/Helper.jal','lib/a/sub/Helper.jal']]));
test('collisions and invalid destinations are rejected before mutation',()=>{for(const [paths,old,to,folder] of [[['a/Main.jal','b/Main.jal'],'a','b',true],[['Main.jal','Other.jal'],'Other.jal','Main.jal',false],[['Main.jal','Other.jal'],'Other.jal','Main.jal/Other.jal',false],[['a/Main.jal'],'a','a/child',true],[['a/Main.jal'],'a','../a',true]])assert.throws(()=>planPathChange(paths,old,to,folder));});
